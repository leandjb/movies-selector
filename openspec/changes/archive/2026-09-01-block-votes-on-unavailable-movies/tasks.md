## 1. State-layer gating (winner.js, app.js core)

- [x] 1.1 In `src/winner.js` `tallyResults`: filter out movies with status `loading` or `error` before building rows; run the empty-board check on the filtered list; add an early return `{ ok: false, reason: "no-votable-movies" }` checked before the missing-votes gate; keep the missing-votes gate comparing the filtered total against the full budget — verify `pnpm test tests/unit/winner.test.js` still passes (status-less helper rows must keep tallying) and add unit tests: error/loading movie never wins or appears in rows; percentages computed over filtered total only; votes in `votesById` for a filtered-out id are ignored by both total and gate; all-error board returns `no-votable-movies`; status-less rows still tally
- [x] 1.2 In `src/app.js`: add the `isVotable(movie)` predicate (`status !== "loading" && status !== "error"`) and gate `addVote`/`removeVote` with a board lookup so a non-votable movie can never change allocation — verify with a unit-style test that `dispatchEvent(new MouseEvent("click", { bubbles: true }))` on a disabled vote button of an error card changes neither `state.byId`, the counter text, nor persisted `movieVotes.v1`
- [x] 1.3 In `src/app.js` `cardHtml`: gate `canInc = isVotable(m) && remaining() > 0` and `canDec = isVotable(m) && votes > 0` (native `disabled` attribute; counter stays visible and rendered on non-ready cards) — verify via `tests/ui/cards.test.js`: a card hydrated with `statusRoute` failure renders both vote buttons `disabled` with the counter visible

## 2. Vote stripping (free stranded votes)

- [x] 2.1 In `src/app.js`: add `stripUnvotableVotes()` (delete every `state.byId[id]` whose board movie is not votable, return whether anything changed) and call it in the boot sequence in the pinned order `board.load()` → `pruneOrphanVotes()` → `stripUnvotableVotes()` → `saveState()` → `render()` — verify with a seeded-storage test (`loadApp({ clear: false })` after writing `shortlistBoard.v1` with a `status: "error"` row and `movieVotes.v1` votes on it): before-first-paint DOM shows the button disabled and score 0, the pill includes the freed votes, and the persisted `movieVotes.v1.byId` no longer holds the entry
- [x] 2.2 Wire the mid-session strip into the hydration-rejection handler (`board.hydrate(id, null)` → strip → `hydrateCard` → `renderTopbar()` → info toast "Votes returned — <title> didn't load." only when votes were actually freed) — verify via `tests/ui/cards.test.js`: vote on a loading card is impossible, so seed votes via storage on a card whose fetch then fails through the router and assert counter → 0, pill updates without reload, toast text; assert boot failure produces no toast
- [x] 2.3 Add the pill/progress live-update assertion (spec "Freed votes update the navbar immediately") to `tests/ui/sections.test.js`: after a mid-session strip the missing-votes pill and budget progress reflect the freed votes without any other user interaction

## 3. In-place card sync (hydrateCard) + focus

- [x] 3.1 Extract the vote-cluster markup into a shared helper used by both `cardHtml` and `hydrateCard`; extend `hydrateCard` to set/clear button `disabled` per `isVotable`, rewrite `.vote__score`, recompute the counter arc `stroke-dasharray`, call `renderTopbar()` when allocation changed, and drop the dead `menu__card--loading` class removal — verify via `tests/ui/cards.test.js`: a boot-retried error card whose fetch succeeds on reload gets usable vote buttons, score 0, and a live arc (no full re-render; other cards' DOM nodes unchanged)
- [x] 3.2 Focus fallback: in `render({ focusMovieId })` and `hydrateCard`, when the resolved vote button is disabled, move focus to the same card's remove button (focus MUST NOT land on `body`) — verify with a UI test: focus a vote button, force the card to error, assert `document.activeElement` is the card's `[data-remove]` button

## 4. Reveal UX

- [x] 4.1 In `src/app.js` show-winner handler: add the `no-votable-movies` branch with a distinct toast (e.g. "None of tonight's movies loaded — nothing to reveal yet.") different from the missing-votes and empty-board messages; keep the reveal button enabled on a non-empty all-error board — verify via `tests/ui/modals.test.js`: all-error board → no modal, distinct toast; board with one error card holding no votes and a full ready allocation → modal opens with the error movie absent from rows and percentages summing ≈100 over ready rows
- [x] 4.2 Add the stripped-votes-still-missing gate test (spec "Stripped votes stay missing until re-allocated") to `tests/ui/modals.test.js`: after a strip frees votes, reveal stays blocked with the missing-votes toast until the full budget is re-allocated to ready movies, then the modal opens

## 5. Integration + regression

- [x] 5.1 End-to-end test in `tests/integration/pipeline.integration.test.js` (or a new `tests/ui/voting-gates.test.js`): seed a mixed board (ready + loading + error rows, legacy votes on the non-ready ones) → `loadApp({ clear: false })` → assert disabled controls and zeroed counters on non-ready cards, clean pill, clean persisted votes; then fail the retry for one and succeed for another (router `statusRoute` + success route, `flushHydration`) and assert the error card stays locked while the retried card becomes votable at 0 votes
- [x] 5.2 Optional cheap hardening (no behavior change): seed `trimExcess`'s reduce with the first board movie so a corrupt empty-board state cannot throw — verify `pnpm test` passes
- [x] 5.3 Full suite + lint: `pnpm test` passes with zero regressions across `tests/unit/winner.test.js`, `tests/unit/board.test.js`, `tests/ui/*`, `tests/integration/*`
- [x] 5.4 If any CSS adjustment proved necessary during implementation: follow the Impeccable system (`DESIGN.md` / `.impeccable/design.json` tokens, no new palette/easing) and run `pnpm impeccable detect index.html styles.css` — zero unexplained findings; if no CSS changed, record that check as satisfied by inspection (no styles touched)
